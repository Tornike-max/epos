import { AnimatedTree } from "../ui/AnimatedTree";

const HomePage = () => {
  return (
    <div className="h-screen flex justify-center items-center ">
      <AnimatedTree />
    </div>
  );
};

export default HomePage;
